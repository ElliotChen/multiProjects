
apply plugin: 'war'

description = 'backend Maven Webapp'
dependencies {
  compile project(':core')
    compile group: 'org.springframework', name: 'spring-webmvc', version:'5.0.7.RELEASE'
    providedCompile group: 'javax.servlet', name: 'javax.servlet-api', version:'4.0.1'
}
